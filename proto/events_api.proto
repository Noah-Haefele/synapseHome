syntax = "proto3";
package synapsed.api.events;

service EventService {
    rpc Subscribe(SubscribeRequest) returns (stream Event);
}

message SubscribeRequest {
    string topic = 1;
}

message Event {
    string event_type = 1;
}
