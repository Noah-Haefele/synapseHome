[package]
name = "synapsed"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3.27.0"
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
